export abstract class $Timer {
 protected pointStopTime: number
 protected pointStartTime: number

 protected timeIsUp: boolean

 protected _secods:string = '00'
 protected _minutes:string = '00'
 protected _hourses:string = '00'
 protected _days:string = '00'
}